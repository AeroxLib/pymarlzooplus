# CMT vs TGCNet 实验设计方案

## 一、实验命令模板

### 1. 基础命令结构
```bash
python3 -m pymarlzooplus.main --config=<算法> --env-config=<环境> with <参数>
```

### 2. 具体实验命令

#### 快速验证（Hallway）
```bash
# CMT
python3 -m pymarlzooplus.main --config=cmt --env-config=hallway with t_max=50000 use_cuda=True use_tensorboard=True label="CMT_hallway" seed=1

# TGCNet
python3 -m pymarlzooplus.main --config=tgcnet --env-config=hallway with t_max=50000 use_cuda=True use_tensorboard=True label="TGCNet_hallway" seed=1

# QMIX 基线
python3 -m pymarlzooplus.main --config=qmix --env-config=hallway with t_max=50000 use_cuda=True use_tensorboard=True label="QMIX_hallway" seed=1
```

#### 标准对比（SMAC 3m）
```bash
# CMT
python3 -m pymarlzooplus.main --config=cmt --env-config=sc2 with env_args.map_name=3m t_max=2000000 use_cuda=True use_tensorboard=True label="CMT_3m" seed=1 test_interval=10000 test_nepisode=32

# TGCNet
python3 -m pymarlzooplus.main --config=tgcnet --env-config=sc2 with env_args.map_name=3m t_max=2000000 use_cuda=True use_tensorboard=True label="TGCNet_3m" seed=1 test_interval=10000 test_nepisode=32
```

#### 多种子对比（3个种子）
```bash
# CMT seed=1,2,3
for seed in 1 2 3; do
  python3 -m pymarlzooplus.main --config=cmt --env-config=sc2 with env_args.map_name=3m t_max=2000000 use_cuda=True use_tensorboard=True label="CMT_3m_seed${seed}" seed=$seed test_interval=10000
done
```

## 二、TensorBoard 配置

### 当前命名规则
代码自动生成：`{name}_seed{seed}_{map_name}_{datetime}`

例如：`cmt_seed1_3m_2024-01-01-12-00-00`

### 自定义名称方法

#### 方法1：通过 label 参数
```bash
with label="CMT_ablation_noMOA"
```

#### 方法2：查看 TensorBoard
```bash
tensorboard --logdir=results/tb_logs/
```

## 三、CUDA 配置

### 命令行启用
```bash
with use_cuda=True use_cuda_cnn_modules=True
```

### 显存不足时的调整
```bash
with batch_size=16  # 减小batch_size
with buffer_cpu_only=True  # buffer放CPU
```

## 四、关键超参数对比

| 参数 | CMT | TGCNet | 建议统一值 |
|------|-----|--------|-----------|
| hidden_dim | 64 | 128 | 64或128 |
| buffer_size | 100 | 5000 | 按算法默认值 |
| batch_size | 2 | 32 | 按算法默认值 |
| lr | 0.0005 | 0.0005 | 0.0005 |
| gamma | 0.99 | 0.99 | 0.99 |
| test_interval | 10000 | 2000 | 10000 |
| test_nepisode | 32 | 10 | 32 |

## 五、消融实验设计

### CMT 消融
```bash
# CMT w/o Top-K (k_budget=n_agents)
python3 -m pymarlzooplus.main --config=cmt --env-config=hallway with k_budget=3 t_max=50000 use_cuda=True label="CMT_noTopK"

# CMT w/o MAGI (beta=0)
python3 -m pymarlzooplus.main --config=cmt --env-config=hallway with beta=0 t_max=50000 use_cuda=True label="CMT_noMAGI"

# CMT w/o MOA (lambda_c=0)
python3 -m pymarlzooplus.main --config=cmt --env-config=hallway with lambda_c=0 t_max=50000 use_cuda=True label="CMT_noMOA"
```

## 六、注意事项

### 1. 实验前检查清单
- [ ] CUDA 可用：`python -c "import torch; print(torch.cuda.is_available())"`
- [ ] 显存足够：`nvidia-smi`
- [ ] 环境正确：`conda activate cmt_env`

### 2. 显存管理
```bash
# 如果OOM，添加参数
with batch_size=16 buffer_cpu_only=True
```

### 3. 断点续训
```bash
with checkpoint_path="results/models/500000"
```

### 4. 保存模型
```bash
with save_model=True save_model_interval=500000
```

### 5. 日志目录
```
results/sacred/<alg>/<env>/<run_id>/
- config.json: 完整配置
- metrics.json: 训练指标
- cout.txt: 终端输出
```

## 七、推荐实验流程

### Phase 1: 快速验证（1小时内）
- Hallway, t_max=50000, 1个种子
- 验证所有算法能跑通
- 检查 tensorboard 记录

### Phase 2: 标准对比（半天-1天）
- SMAC 3m, t_max=500000, 3个种子
- 正式对比性能
- 绘制学习曲线

### Phase 3: 完整实验（1-3天）
- 多个SMAC地图（3m, 8m, 2s3z）
- t_max=2000000, 3-5个种子
- 消融实验

## 八、监控指标

在 tensorboard 中关注：
- `test_return_mean`: 测试胜率/回报
- `test_return_std`: 稳定性
- `loss`: 训练损失
- `epsilon`: 探索率衰减
- `grad_norm`: 梯度裁剪

## 九、实验记录模板

| 实验 | 算法 | 环境 | 种子 | t_max | 最终性能 | 备注 |
|------|------|------|------|-------|---------|------|
| Exp1 | CMT | 3m | 1 | 2M | ? | 基准 |
| Exp2 | TGCNet | 3m | 1 | 2M | ? | 对比 |
| ... | ... | ... | ... | ... | ... | ... |
